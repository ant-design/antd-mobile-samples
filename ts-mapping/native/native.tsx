import { Card, List } from 'antd-mobile'
import Flex from 'antd-mobile/lib/flex'

const style = { color: 'white' }

const card = (
  <Card
    onLayout={() => {}}
    onTouchEnd={() => { }}
  />
)

const list = () => (
  <List
    style={style}
    onLayout={() => {}}
    onTouchEnd={() => { }}
  />
)
